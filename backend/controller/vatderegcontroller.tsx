// import VATDereg from "../models/VATDereg.js";

// export const create = async (req, res) => {
//   try {
//     const vatDeregData = new VATDereg(req.body);
//     const savedData = await vatDeregData.save();
//     res.status(201).json(savedData);
//   } catch (error) {
//     res.status(500).json({ error: error.message || "Server Error" });
//   }
// };

